{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local --docker -f mememaker --logger --path mock/request.json",
    "invoke": "sls invoke -f mememaker --docker --logger --path mock/request.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "axios": "^0.21.1"
  }
}
